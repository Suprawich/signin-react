import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  typography: {
    fontFamily: 'Kanit, sans-serif',
  },
  // ... ตัวอย่างอื่น ๆ ของการตั้งค่า theme ...
});

export default theme;
